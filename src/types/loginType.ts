export type LoginType = {
  email: string;
  password: string;
};

export type UserProfile = {
  id: number;
  user_id: string;
  username: string;
  email: string;
  avatar_url: string;
  bio: string;
  createdAt: string;
};

